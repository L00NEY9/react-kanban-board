import React from 'react';
import logotypeImage from "../../../assets/logo.svg"

const Logotype = () => {
  return (
    <img width={131} height={32} src={logotypeImage} alt="logotype"/>
  );
};

export default Logotype;
